# Bitrix24 Configuration# Bitrix24 Configuration

# Replace with your actual Bitrix24 webhook URL (without the /crm.lead.add.json part)# Replace with your actual Bitrix24 webhook URL

# Example: https://your-domain.bitrix24.com/rest/123/webhook_token# Example: https://your-domain.bitrix24.com/rest/123/webhook_token

VITE_BITRIX24_WEBHOOK_URL=https://your-domain.bitrix24.com/rest/YOUR_USER_ID/YOUR_WEBHOOK_TOKENREACT_APP_BITRIX24_WEBHOOK_URL=https://your-domain.bitrix24.com/rest/YOUR_USER_ID/YOUR_WEBHOOK_TOKEN



# Example configuration:# Example configuration:

# VITE_BITRIX24_WEBHOOK_URL=https://mydomain.bitrix24.com/rest/1/abc123def456# REACT_APP_BITRIX24_WEBHOOK_URL=https://mydomain.bitrix24.com/rest/1/abc123def456



# To get your webhook URL:# To get your webhook URL:

# 1. Go to your Bitrix24 portal# 1. Go to your Bitrix24 portal

# 2. Navigate to Applications > Webhooks > REST-webhooks# 2. Navigate to Applications > Webhooks > REST-webhooks

# 3. Create a new incoming webhook# 3. Create a new incoming webhook

# 4. Select the permissions you need (crm.lead.add, crm.lead.update, etc.)# 4. Select the permissions you need (crm.lead.add, crm.lead.update, etc.)

# 5. Copy the generated webhook URL (without the method part)# 5. Copy the generated webhook URL